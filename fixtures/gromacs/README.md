# GROMACS fixtures

This directory contains small `dhdl.xvg` fixtures used to exercise the GROMACS parser paths.

The current fixture is intentionally minimal and is designed for parser integration coverage rather than numerical regression against an external reference tool.
